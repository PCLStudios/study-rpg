# Study RPG

Frontend-only Study RPG built with Next.js + Tailwind (Apple-style dark UI).

Run locally:

```bash
npm install
npm run dev
```

Pages:
- `/` dashboard
- `/shop` spins & shop
- `/collection` placeholder API results
- `/inventory` unlocked items (localStorage)

Persistent data: `sp` and `inventory` are stored in `localStorage`.
